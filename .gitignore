# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local uploads / scratch
uploads/
*.local

# SQLite database + signing secret (created at runtime)
data/*.db
data/.lesarin-secret

# Site identity — the private half never leaves the box (see site_agent/identity.py)
data/site_key.pem

# Frontend build / deps
frontend/node_modules/
frontend/dist/
frontend/.angular/
