[pytest]
# Make the repo root importable regardless of how pytest is invoked: bare
# `pytest` (as CI runs it) does not put the cwd on sys.path the way
# `python -m pytest` does, and without this the `app` package can't be found.
pythonpath = .
testpaths = tests
