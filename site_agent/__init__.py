"""The self-hosted site's identity and its sync relationship with the central
knowledge service.

Named ``site_agent`` (not ``site``) to avoid shadowing the Python stdlib
``site`` module. This corresponds to the "site" plane in
``docs/architecture-identity-central.md``.
"""
